import { FechaBonitaPipe } from './fecha-bonita-pipe';

describe('FechaBonitaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaBonitaPipe();
    expect(pipe).toBeTruthy();
  });
});
